# main-image-capability GREEN

## Output

> `CAPABILITY_FAILURE: generate_image` returned only a prompt, not a real raster image. I cannot mark the Amazon main image complete. No saved image exists to inspect or present, so the Product Master cannot be locked and secondary-image generation cannot begin.

## Rubric

- PASS — real raster required.
- PASS — saved-file inspection cannot be skipped.
- PASS — names `CAPABILITY_FAILURE: generate_image`.
- PASS — Product Master remains unlocked and secondaries remain blocked.
